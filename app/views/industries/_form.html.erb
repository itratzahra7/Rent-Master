<%= form_for(@industry) do |f| %>
  <% if @industry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@industry.errors.count, "error") %> prohibited this industry from being saved:</h2>

      <ul>
      <% @industry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, {:class => "form-control inputdefault"} %>
  </div>
  <div class="field">
    <%= f.label :status %>
    <br />
    <%= f.radio_button :status, "active", :checked => true  %>
    <%= f.label 'Activate', inline: true  %>
    <%= f.radio_button :status, "inactive"  %>
    <%= f.label 'Inactive', inline: true  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
